# createqr

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts -- "URL"
bun run index.ts -- "URL" -p "PATH"
```

This project was created using `bun init` in bun v1.3.8. [Bun](https://bun.com) is a fast all-in-one JavaScript runtime.
